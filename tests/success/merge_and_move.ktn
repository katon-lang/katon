func main(a: [3; int], cond: bool) {
    let b = a; 
    let c: [3; int]; // Size is mandatory here
    
    if cond {
        c = b; // OK: [3; int] assigned to [3; int]
    } else {
        c = [1, 2, 3]; // OK: Literal size matches 3
    }
    
    // Static verification: c_length is now a known constant 3
    let len = 3; 
    ensures len >= 0;
}